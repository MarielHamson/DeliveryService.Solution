@* @using PackageDelivery.Models; *@

<h1> Here is your delivery estimate (lowest price GUARANTEED)</h1>
@* @Model == newParcel *@
@* @if (Model.Count == 0)
{
  <p> You have not entered any dimensions, please go back and fix this.</p>
} *@
@* @foreach (var parcel in Model)
{

} *@

<p> Your estimate is: $ @Model.findPrice(Model.findVolume())</p>